#include <bits/stdc++.h>
#include <string>
#include <vector>
#include <stdio.h>
#include <stdlib.h>
using namespace std;

int main() {
	int Q;
	cin >> Q;

    vector <string> query(Q*3, "q");
    int a;
    cin >> a;

    for (int i=0; i<Q*3; i++) {
        
    }

	return 0;
}
